<nav class="navbar" role="navigation" aria-label="main navigation">
	<div class="navbar-brand">
		<a class="navbar-item" href="{{ path('app_home') }}">
			Don'EAT !
		</a>

		<a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
			<span aria-hidden="true"></span>
			<span aria-hidden="true"></span>
			<span aria-hidden="true"></span>
			<span aria-hidden="true"></span>
		</a>
	</div>

	<div id="navbarBasicExample" class="navbar-menu">
		<div class="navbar-start">
			<a class="navbar-item" href="{{ path('app_home') }}">
				Accueil
			</a>

			{% if app.user %}
			<a class="navbar-item" href="{{ path('app_meal') }}">
				Mes repas
			</a>
			
			<div class="navbar-item has-dropdown is-hoverable">
				<a class="navbar-link">
					Mes réservations
				</a>
				<div class="navbar-dropdown">
					<a class="navbar-item" href="{{ path('app_booking_request') }}">
						Gestion des réservations en attente
					</a>
					<a class="navbar-item" href="{{ path('app_booking_archives') }}">
						Archive des demandes
					</a>
				</div>
			</div>

			{% if is_granted('ROLE_ADMIN') %}

			<div class="navbar-item has-dropdown is-hoverable">
				<a class="navbar-link">
					Paramétrage
				</a>
				<div class="navbar-dropdown">
					<a class="navbar-item" href="{{ path('app_mealtag') }}">
						Tag repas (allergènes)
					</a>
					<a class="navbar-item" href="{{ path('app_user') }}">
						Gestion des utilisateurs
					</a>
				</div>
			</div>
			{% endif %}
			
			{% endif %}

			<div class="navbar-item has-dropdown is-hoverable">
				<a class="navbar-link">
					Aide
				</a>
				<div class="navbar-dropdown">
					<a class="navbar-item">
						A Propos
					</a>
					<a class="navbar-item">
						Contact
					</a>
				</div>
			</div>
		</div>

		<div class="navbar-end">
			{% if app.user %}

				<div class="navbar-item has-dropdown is-hoverable">
					<a class="navbar-link">
						Mon profil
					</a>
					<div class="navbar-dropdown">
						<a class="navbar-item" href="{{ path('app_profile') }}">
							Gérer mon compte
						</a>
						<a class="navbar-item" href="{{ path('app_logout') }}">
							Se déconnecter
						</a>
					</div>
				</div>
			{% else %}


				<div class="navbar-item">

					<div class="buttons">
						<a class="button is-primary" href="{{ path('app_register') }}">
							<strong>S'inscrire</strong>
						</a>
						<a class="button is-light" href="{{ path('app_login') }}">
							Se connecter
						</a>
					</div>
        
        {% endif %}
				</div>
			</div>
		</div>
	</nav>
